import 'package:flutter/cupertino.dart';

class AppNavScreen extends StatelessWidget {
  const AppNavScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
